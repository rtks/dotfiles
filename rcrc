EXCLUDES="README.md install.sh rcm vscode"
SYMLINK_DIRS="powerline"
UNDOTTED="bin"

if [ -r ~/.rcrc.local ]; then
  source ~/.rcrc.local
else
  if [ -r ~/.dotfiles.local/rcrc.local ]; then
    source ~/.dotfiles.local/rcrc.local
  fi
fi
